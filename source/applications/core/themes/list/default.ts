import { green, magenta, yellow, bold } from "../../../../imports/color.ts";
import { Theme } from "../type.ts";

export const Default: Theme = {
    CurrentFolderColor: green,
    UserNameColor: magenta,
    UserLinkColor: yellow,
    AnswerColor: green,
    InputColor: bold,
};
